# === Audiarr Example Environment ===
# Copy this to .env and fill in your own values

# ── Database Settings ─────────────────────
POSTGRES_USER=audiarr
POSTGRES_PASSWORD=change_me
POSTGRES_DB=audiarr

# ── API Settings ──────────────────────────
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
SETTINGS_PATH=/app/config/settings.json

# ── Host Ports ────────────────────────────
API_PORT=5080
WEB_PORT=3080

# ── Local Paths (edit for your system) ────
AUDIOBOOKS_PATH=/path/to/audiobooks
AUDIARR_PATH=/path/to/audiarr
CONFIG_PATH=${AUDIARR_PATH}/data/config
DOWNLOADS_PATH=${AUDIARR_PATH}/data/downloads
